Options -Indexes
<IfModule mod_rewrite.c>
RewriteEngine On
# Route to php -S or direct files if using Apache; fallback to index
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [L]
</IfModule>